<div class="row">
<div class="span14 columns offset1">

<h2>Ваш аккаунт, <%= current_user.name %></h2><br />

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f| %>

  <%= f.text_field :name %></p>
  <%= f.email_field :email %></p>

  <%= f.password_field :password, :info => "не заполняйте, если не хотите менять пароль" %>
  <%= f.password_field :password_confirmation, :info => "не заполняйте, если не хотите менять пароль" %>

  <%= f.password_field :current_password, :info => "укажите ваш текущий пароль чтобы подтвердить изменения" %>

  <div class="actions">
    <%= f.submit "Обновить", :class => "btn primary" %>
  </div>
<% end %>

<h3>Удалить мой аккаунт</h3>

<p>Не понравилось? <%= link_to "Удалите ваш аккаунт", registration_path(resource_name), :confirm => "Вы уверены?", :method => :delete %>.</p>

<%= link_to "Назад", :back %>

</div>
</div>
